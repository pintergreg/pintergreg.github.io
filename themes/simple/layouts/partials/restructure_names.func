{{ $result := (slice) }}
{{- range . }}
    {{ $family := index (split .name ", ") 0 }}
    {{ $given := index (split .name ", ") 1 }}
    {{ $result = $result | append (printf "%s %s" $given $family) }}
{{- end -}}
{{return $result }}
